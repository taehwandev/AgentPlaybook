.DS_Store
.antigravitycli/
*.log
.agentplaybook/
node_modules/
dist/
build/
.env
.env.*
!.env.example
# VibeGuard local secrets
!.env.sample
!.env.template
!.env.dist
!.env.defaults
!.env.schema
!.env*.default
!.env*.defaults
!.env*.dist
!.env*.example
!.env*.sample
!.env*.schema
!.env*.template
.env.vibeguard.local
# VibeGuard local state
.vibeguard/

# Wikimap document-search index. Source docs and the pinned adapter stay tracked;
# the deterministic SQLite cache is rebuilt locally by workflow.py.
.wikimap/

# Graphify local cache/runtime state. Keep current curated graph outputs visible.
graphify-out/cache/
graphify-out/.graphify_*
graphify-out/**/.graphify_*
graphify-out/cost.json
graphify-out/**/cost.json
graphify-out/[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]/

# agentplaybook-project-assets:start
# Runtime evidence is local, but canonical project skills are repository assets.
!.agentplaybook/
.agentplaybook/*
!.agentplaybook/.gitignore
!.agentplaybook/skills/
.agentplaybook/skills/*
!.agentplaybook/skills/graphify/
!.agentplaybook/skills/graphify/**
# agentplaybook-project-assets:end

# Evidence emitted from a nested working directory is always local runtime state.
*/.agentplaybook/
